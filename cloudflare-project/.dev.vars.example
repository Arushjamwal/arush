# For local testing only, via `npx wrangler pages dev`.
# Copy this file to .dev.vars and paste your real key in.
# For the deployed site, set this in the Cloudflare dashboard instead
# (Pages project > Settings > Environment variables) — this file is never uploaded.

ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
